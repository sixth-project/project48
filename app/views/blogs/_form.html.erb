
<%= tinymce :relative_urls => false %>
<%= form_for(blog) do |f| %>

  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<p>
  <%= f.label :title %>
  <%= f.text_field :title %>
</p>

<p>
  <%= f.label :text %>
  <%= f.text_area :text, :class => "tinymce", :rows => 100, :cols => 120 %>
  <%= tinymce :text_css => asset_path('application.css')%>
</p>

<p>
  <%= f.submit %>
</P>
  <% end %>
